# key: form-controller
# name: Controller (antd)
# --
<Controller
  name="$1"
  rules={{
    required: 'campo requerido',
  }}
  render={({ field }) => (
    <Form.Item
      label="$2"
      validateStatus={(errors?.$1) && 'error'}
    >
      $0
    </Form.Item>
  )}
/>